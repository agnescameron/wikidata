### small wikidata -> jsonld scraper

If you want to run this on an arbitrary entity, change the variable 'entity' at the top of the file. To run the code:

```
mkdir package
npm install
node wikidata.js
```
